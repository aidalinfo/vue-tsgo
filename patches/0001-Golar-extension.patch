From d8cc3b375992b387c9e2909898ed9b81c022d016 Mon Sep 17 00:00:00 2001
From: Nikhil Verma <nikhilgotmail@gmail.com>
Date: Mon, 15 Dec 2025 12:37:25 +0300
Subject: [PATCH 1/2] Golar extension

---
 cmd/tsgo/lsp.go                         |  5 ++++-
 cmd/tsgo/sys.go                         |  8 +++++++-
 internal/ast/ast.go                     | 12 ++++++++++++
 internal/compiler/program.go            | 23 +++++++++++++++++++++--
 internal/execute/incremental/program.go |  6 +++++-
 internal/execute/tsc.go                 |  2 ++
 internal/execute/tsc/compile.go         |  3 +++
 internal/execute/tsctests/sys.go        |  6 ++++++
 internal/fourslash/fourslash.go         | 19 +++++++++++++------
 internal/golarext/golarext.go           | 15 +++++++++++++++
 internal/ls/diagnostics.go              |  1 +
 internal/ls/host.go                     |  4 ++++
 internal/ls/hover.go                    |  2 +-
 internal/ls/lsconv/converters.go        |  2 +-
 internal/lsp/server.go                  |  9 +++++++++
 internal/module/resolver.go             |  8 ++++++++
 internal/module/util.go                 |  5 +++++
 internal/project/autoimport.go          |  2 +-
 internal/project/compilerhost.go        |  2 +-
 internal/project/parsecache.go          | 21 ++++++++++++++++-----
 internal/project/session.go             |  6 +++++-
 internal/project/snapshot.go            |  6 ++++++
 internal/tspath/extension.go            | 12 ++++++++++++
 23 files changed, 158 insertions(+), 21 deletions(-)
 create mode 100644 internal/golarext/golarext.go

diff --git a/cmd/tsgo/lsp.go b/cmd/tsgo/lsp.go
index dfeac660d..45c89e7c8 100644
--- a/cmd/tsgo/lsp.go
+++ b/cmd/tsgo/lsp.go
@@ -10,6 +10,7 @@ import (
 	"runtime"
 	"syscall"
 
+	"github.com/NikhilVerma/vue-tsgo/golar"
 	"github.com/microsoft/typescript-go/internal/bundled"
 	"github.com/microsoft/typescript-go/internal/core"
 	"github.com/microsoft/typescript-go/internal/lsp"
@@ -41,11 +42,13 @@ func runLSP(args []string) int {
 		defer profileSession.Stop()
 	}
 
-	fs := bundled.WrapFS(osvfs.FS())
+	fs := golar.WrapFS(bundled.WrapFS(osvfs.FS()))
 	defaultLibraryPath := bundled.LibPath()
 	typingsLocation := getGlobalTypingsCacheLocation()
 
 	s := lsp.NewServer(&lsp.ServerOptions{
+		GolarCallbacks: golar.GolarExtCallbacks,
+
 		In:                 lsp.ToReader(os.Stdin),
 		Out:                lsp.ToWriter(os.Stdout),
 		Err:                os.Stderr,
diff --git a/cmd/tsgo/sys.go b/cmd/tsgo/sys.go
index e5b2b8d56..ae2217c04 100644
--- a/cmd/tsgo/sys.go
+++ b/cmd/tsgo/sys.go
@@ -6,8 +6,10 @@ import (
 	"os"
 	"time"
 
+	"github.com/NikhilVerma/vue-tsgo/golar"
 	"github.com/microsoft/typescript-go/internal/bundled"
 	"github.com/microsoft/typescript-go/internal/execute/tsc"
+	"github.com/microsoft/typescript-go/internal/golarext"
 	"github.com/microsoft/typescript-go/internal/tspath"
 	"github.com/microsoft/typescript-go/internal/vfs"
 	"github.com/microsoft/typescript-go/internal/vfs/osvfs"
@@ -59,6 +61,10 @@ func (s *osSys) GetEnvironmentVariable(name string) string {
 	return os.Getenv(name)
 }
 
+func (s *osSys) GetGolarCallbacks() *golarext.GolarCallbacks {
+	return golar.GolarExtCallbacks
+}
+
 func newSystem() *osSys {
 	cwd, err := os.Getwd()
 	if err != nil {
@@ -68,7 +74,7 @@ func newSystem() *osSys {
 
 	return &osSys{
 		cwd:                tspath.NormalizePath(cwd),
-		fs:                 bundled.WrapFS(osvfs.FS()),
+		fs:                 golar.WrapFS(bundled.WrapFS(osvfs.FS())),
 		defaultLibraryPath: bundled.LibPath(),
 		writer:             os.Stdout,
 		start:              time.Now(),
diff --git a/internal/ast/ast.go b/internal/ast/ast.go
index 23a4badf3..b7417410e 100644
--- a/internal/ast/ast.go
+++ b/internal/ast/ast.go
@@ -10747,6 +10747,10 @@ type SourceFile struct {
 	LocalsContainerBase
 	compositeNodeBase
 
+	GolarLanguageData any
+	WrapSemanticDiagnostics func ([]*Diagnostic) []*Diagnostic
+	WrapDiagnostics func ([]*Diagnostic) []*Diagnostic
+
 	// Fields set by NewSourceFile
 	fileName       string // For debugging convenience
 	parseOptions   SourceFileParseOptions
@@ -10831,10 +10835,18 @@ func (node *SourceFile) Text() string {
 	return node.text
 }
 
+func (node *SourceFile) SetText(text string) {
+	node.text = text
+}
+
 func (node *SourceFile) FileName() string {
 	return node.parseOptions.FileName
 }
 
+func (node *SourceFile) SetParseOptions(opts SourceFileParseOptions) {
+	node.parseOptions = opts
+}
+
 func (node *SourceFile) Path() tspath.Path {
 	return node.parseOptions.Path
 }
diff --git a/internal/compiler/program.go b/internal/compiler/program.go
index 92cc16829..afa5cd2b9 100644
--- a/internal/compiler/program.go
+++ b/internal/compiler/program.go
@@ -475,7 +475,13 @@ func (p *Program) collectDiagnosticsFromFiles(ctx context.Context, sourceFiles [
 
 func (p *Program) GetSyntacticDiagnostics(ctx context.Context, sourceFile *ast.SourceFile) []*ast.Diagnostic {
 	return p.collectDiagnostics(ctx, sourceFile, false /*concurrent*/, func(_ context.Context, file *ast.SourceFile) []*ast.Diagnostic {
-		return core.Concatenate(file.Diagnostics(), file.JSDiagnostics())
+		diags := file.Diagnostics()
+		jsDiags := file.JSDiagnostics()
+		if file.WrapSemanticDiagnostics != nil {
+			diags = file.WrapDiagnostics(diags)
+			jsDiags = file.WrapDiagnostics(jsDiags)
+		}
+		return core.Concatenate(diags, jsDiags)
 	})
 }
 
@@ -486,7 +492,11 @@ func (p *Program) GetBindDiagnostics(ctx context.Context, sourceFile *ast.Source
 		p.BindSourceFiles()
 	}
 	return p.collectDiagnostics(ctx, sourceFile, false /*concurrent*/, func(_ context.Context, file *ast.SourceFile) []*ast.Diagnostic {
-		return file.BindDiagnostics()
+		diags := file.BindDiagnostics()
+		if file.WrapSemanticDiagnostics != nil {
+			diags = file.WrapDiagnostics(diags)
+		}
+		return diags
 	})
 }
 
@@ -519,6 +529,9 @@ func (p *Program) GetIncludeProcessorDiagnostics(sourceFile *ast.SourceFile) []*
 		return nil
 	}
 	filtered, _ := p.getDiagnosticsWithPrecedingDirectives(sourceFile, p.includeProcessor.getDiagnostics(p).GetDiagnosticsForFile(sourceFile.FileName()))
+	if sourceFile.WrapDiagnostics != nil {
+		filtered = sourceFile.WrapDiagnostics(filtered)
+	}
 	return filtered
 }
 
@@ -1131,6 +1144,9 @@ func (p *Program) getBindAndCheckDiagnosticsForFile(ctx context.Context, sourceF
 
 	isPlainJS := ast.IsPlainJSFile(sourceFile, compilerOptions.CheckJs)
 	if isPlainJS {
+		if sourceFile.WrapSemanticDiagnostics != nil {
+			diags = sourceFile.WrapSemanticDiagnostics(diags)
+		}
 		return core.Filter(diags, func(d *ast.Diagnostic) bool {
 			return plainJSErrors.Has(d.Code())
 		})
@@ -1150,6 +1166,9 @@ func (p *Program) getBindAndCheckDiagnosticsForFile(ctx context.Context, sourceF
 			filtered = append(filtered, ast.NewDiagnostic(sourceFile, directive.Loc, diagnostics.Unused_ts_expect_error_directive))
 		}
 	}
+	if sourceFile.WrapSemanticDiagnostics != nil {
+		filtered = sourceFile.WrapSemanticDiagnostics(filtered)
+	}
 	return filtered
 }
 
diff --git a/internal/execute/incremental/program.go b/internal/execute/incremental/program.go
index c1e581046..7b575b5e6 100644
--- a/internal/execute/incremental/program.go
+++ b/internal/execute/incremental/program.go
@@ -178,8 +178,12 @@ func (p *Program) getSemanticDiagnosticsOfFile(file *ast.SourceFile) []*ast.Diag
 	if !ok {
 		panic("After handling all the affected files, there shouldnt be more changes")
 	}
+	diags := cachedDiagnostics.getDiagnostics(p.program, file)
+	if file.WrapDiagnostics != nil {
+		diags = file.WrapDiagnostics(diags)
+	}
 	return slices.Concat(
-		compiler.FilterNoEmitSemanticDiagnostics(cachedDiagnostics.getDiagnostics(p.program, file), p.snapshot.options),
+		compiler.FilterNoEmitSemanticDiagnostics(diags, p.snapshot.options),
 		p.program.GetIncludeProcessorDiagnostics(file),
 	)
 }
diff --git a/internal/execute/tsc.go b/internal/execute/tsc.go
index 0827b6919..8f455f11a 100644
--- a/internal/execute/tsc.go
+++ b/internal/execute/tsc.go
@@ -265,6 +265,7 @@ func performIncrementalCompilation(
 	testing tsc.CommandLineTesting,
 ) tsc.CommandLineResult {
 	host := compiler.NewCachedFSCompilerHost(sys.GetCurrentDirectory(), sys.FS(), sys.DefaultLibraryPath(), extendedConfigCache, getTraceFromSys(sys, config.Locale(), testing))
+	host = sys.GetGolarCallbacks().WrapCompilerHost(host)
 	buildInfoReadStart := sys.Now()
 	oldProgram := incremental.ReadBuildInfoProgram(config, incremental.NewBuildInfoReader(host), host)
 	compileTimes.BuildInfoReadTime = sys.Now().Sub(buildInfoReadStart)
@@ -308,6 +309,7 @@ func performCompilation(
 	testing tsc.CommandLineTesting,
 ) tsc.CommandLineResult {
 	host := compiler.NewCachedFSCompilerHost(sys.GetCurrentDirectory(), sys.FS(), sys.DefaultLibraryPath(), extendedConfigCache, getTraceFromSys(sys, config.Locale(), testing))
+	host = sys.GetGolarCallbacks().WrapCompilerHost(host)
 	// todo: cache, statistics, tracing
 	parseStart := sys.Now()
 	program := compiler.NewProgram(compiler.ProgramOptions{
diff --git a/internal/execute/tsc/compile.go b/internal/execute/tsc/compile.go
index 4fdd27f7c..3f52f4511 100644
--- a/internal/execute/tsc/compile.go
+++ b/internal/execute/tsc/compile.go
@@ -9,12 +9,15 @@ import (
 	"github.com/microsoft/typescript-go/internal/compiler"
 	"github.com/microsoft/typescript-go/internal/diagnostics"
 	"github.com/microsoft/typescript-go/internal/execute/incremental"
+	"github.com/microsoft/typescript-go/internal/golarext"
 	"github.com/microsoft/typescript-go/internal/locale"
 	"github.com/microsoft/typescript-go/internal/tspath"
 	"github.com/microsoft/typescript-go/internal/vfs"
 )
 
 type System interface {
+	GetGolarCallbacks() *golarext.GolarCallbacks
+
 	Writer() io.Writer
 	FS() vfs.FS
 	DefaultLibraryPath() string
diff --git a/internal/execute/tsctests/sys.go b/internal/execute/tsctests/sys.go
index 05676ef9e..4dc1fc908 100644
--- a/internal/execute/tsctests/sys.go
+++ b/internal/execute/tsctests/sys.go
@@ -9,6 +9,7 @@ import (
 	"sync"
 	"time"
 
+	"github.com/NikhilVerma/vue-tsgo/golar"
 	"github.com/microsoft/typescript-go/internal/collections"
 	"github.com/microsoft/typescript-go/internal/compiler"
 	"github.com/microsoft/typescript-go/internal/core"
@@ -16,6 +17,7 @@ import (
 	"github.com/microsoft/typescript-go/internal/execute"
 	"github.com/microsoft/typescript-go/internal/execute/incremental"
 	"github.com/microsoft/typescript-go/internal/execute/tsc"
+	"github.com/microsoft/typescript-go/internal/golarext"
 	"github.com/microsoft/typescript-go/internal/locale"
 	"github.com/microsoft/typescript-go/internal/testutil/fsbaselineutil"
 	"github.com/microsoft/typescript-go/internal/testutil/harnessutil"
@@ -168,6 +170,10 @@ var (
 	_ tsc.CommandLineTesting = (*TestSys)(nil)
 )
 
+func (s *TestSys) GetGolarCallbacks() *golarext.GolarCallbacks {
+	return golar.GolarExtCallbacks
+}
+
 func (s *TestSys) Now() time.Time {
 	return s.clock.Now()
 }
diff --git a/internal/fourslash/fourslash.go b/internal/fourslash/fourslash.go
index 034c2ec4c..7b2233217 100644
--- a/internal/fourslash/fourslash.go
+++ b/internal/fourslash/fourslash.go
@@ -14,6 +14,7 @@ import (
 	"testing"
 	"unicode/utf8"
 
+	"github.com/NikhilVerma/vue-tsgo/golar"
 	"github.com/google/go-cmp/cmp"
 	"github.com/microsoft/typescript-go/internal/bundled"
 	"github.com/microsoft/typescript-go/internal/collections"
@@ -162,13 +163,14 @@ func newLSPPipe() (*lspReader, *lspWriter) {
 
 const rootDir = "/"
 
+
+func NewFourslash(t *testing.T, capabilities *lsproto.ClientCapabilities, content string) (*FourslashTest, func()) {
 var parseCache = project.NewParseCache(project.RefCountCacheOptions{
 	DisableDeletion: true,
-},
-)
-
-func NewFourslash(t *testing.T, capabilities *lsproto.ClientCapabilities, content string) (*FourslashTest, func()) {
+}, golar.GolarExtCallbacks)
+	if false {
 	repo.SkipIfNoTypeScriptSubmodule(t)
+	}
 	if !bundled.Embedded {
 		// Without embedding, we'd need to read all of the lib files out from disk into the MapFS.
 		// Just skip this for now.
@@ -237,9 +239,11 @@ func NewFourslash(t *testing.T, capabilities *lsproto.ClientCapabilities, conten
 	outputReader, outputWriter := newLSPPipe()
 
 	fsFromMap := vfstest.FromMap(testfs, true /*useCaseSensitiveFileNames*/)
-	fs := bundled.WrapFS(fsFromMap)
+	fs := golar.WrapFourslashFS(testData.GlobalOptions, bundled.WrapFS(fsFromMap))
 
 	server := lsp.NewServer(&lsp.ServerOptions{
+		GolarCallbacks: golar.GolarExtCallbacks,
+
 		In:  inputReader,
 		Out: outputWriter,
 		Err: io.Discard,
@@ -1080,6 +1084,9 @@ func getLanguageKind(filename string) lsproto.LanguageKind {
 	if tspath.FileExtensionIs(filename, tspath.ExtensionJson) {
 		return lsproto.LanguageKindJSON
 	}
+	if tspath.FileExtensionIs(filename, ".vue") {
+		return "vue"
+	}
 	return lsproto.LanguageKindTypeScript // !!! should we error in this case?
 }
 
@@ -3960,7 +3967,7 @@ func (f *FourslashTest) verifyDiagnostics(t *testing.T, expected []*lsproto.Diag
 				t.Fatalf("No ranges found in file %s to assign to diagnostic with empty range", f.activeFilename)
 			}
 			diagWithRange := *diag
-			diagWithRange.Range = rangesInFile[0].LSRange
+			diagWithRange.Range = rangesInFile[i].LSRange
 			expectedWithRanges[i] = &diagWithRange
 		} else {
 			expectedWithRanges[i] = diag
diff --git a/internal/golarext/golarext.go b/internal/golarext/golarext.go
new file mode 100644
index 000000000..1cf615e51
--- /dev/null
+++ b/internal/golarext/golarext.go
@@ -0,0 +1,15 @@
+package golarext
+
+import (
+	"github.com/microsoft/typescript-go/internal/ast"
+	"github.com/microsoft/typescript-go/internal/compiler"
+	"github.com/microsoft/typescript-go/internal/core"
+	"github.com/microsoft/typescript-go/internal/vfs"
+)
+
+type GolarCallbacks struct {
+	WrapDiagnostics func (file *ast.SourceFile, diagnostics []*ast.Diagnostic) []*ast.Diagnostic
+	PositionToService func(file *ast.SourceFile, pos int) int
+	WrapCompilerHost  func(host compiler.CompilerHost) compiler.CompilerHost
+	ParseSourceFile   func(fs vfs.FS, opts ast.SourceFileParseOptions, sourceText string, scriptKind core.ScriptKind) *ast.SourceFile
+}
diff --git a/internal/ls/diagnostics.go b/internal/ls/diagnostics.go
index 52d65512e..922c25564 100644
--- a/internal/ls/diagnostics.go
+++ b/internal/ls/diagnostics.go
@@ -36,6 +36,7 @@ func (l *LanguageService) toLSPDiagnostics(ctx context.Context, diagnostics ...[
 	lspDiagnostics := make([]*lsproto.Diagnostic, 0, size)
 	for _, diagSlice := range diagnostics {
 		for _, diag := range diagSlice {
+			if diag == nil { continue }
 			lspDiagnostics = append(lspDiagnostics, lsconv.DiagnosticToLSPPull(ctx, l.converters, diag, l.UserPreferences().ReportStyleChecksAsWarnings))
 		}
 	}
diff --git a/internal/ls/host.go b/internal/ls/host.go
index 3f1e25aa9..f0c29881a 100644
--- a/internal/ls/host.go
+++ b/internal/ls/host.go
@@ -1,5 +1,7 @@
 package ls
 
+import "github.com/microsoft/typescript-go/internal/golarext"
+
 import (
 	"github.com/microsoft/typescript-go/internal/ls/autoimport"
 	"github.com/microsoft/typescript-go/internal/ls/lsconv"
@@ -8,6 +10,8 @@ import (
 )
 
 type Host interface {
+	GetGolarCallbacks() *golarext.GolarCallbacks
+
 	UseCaseSensitiveFileNames() bool
 	ReadFile(path string) (contents string, ok bool)
 	Converters() *lsconv.Converters
diff --git a/internal/ls/hover.go b/internal/ls/hover.go
index 7a5319c65..6e0504bee 100644
--- a/internal/ls/hover.go
+++ b/internal/ls/hover.go
@@ -25,7 +25,7 @@ func (l *LanguageService) ProvideHover(ctx context.Context, documentURI lsproto.
 	contentFormat := lsproto.PreferredMarkupKind(caps.TextDocument.Hover.ContentFormat)
 
 	program, file := l.getProgramAndFile(documentURI)
-	node := astnav.GetTouchingPropertyName(file, int(l.converters.LineAndCharacterToPosition(file, position)))
+	node := astnav.GetTouchingPropertyName(file, l.host.GetGolarCallbacks().PositionToService(file, int(l.converters.LineAndCharacterToPosition(file, position))))
 	if node.Kind == ast.KindSourceFile {
 		// Avoid giving quickInfo for the sourceFile as a whole.
 		return lsproto.HoverOrNull{}, nil
diff --git a/internal/ls/lsconv/converters.go b/internal/ls/lsconv/converters.go
index 96c0bd352..8a3bc4197 100644
--- a/internal/ls/lsconv/converters.go
+++ b/internal/ls/lsconv/converters.go
@@ -67,7 +67,7 @@ func (c *Converters) ToLSPLocation(script Script, rng core.TextRange) lsproto.Lo
 
 func LanguageKindToScriptKind(languageID lsproto.LanguageKind) core.ScriptKind {
 	switch languageID {
-	case "typescript":
+	case "typescript", "vue":
 		return core.ScriptKindTS
 	case "typescriptreact":
 		return core.ScriptKindTSX
diff --git a/internal/lsp/server.go b/internal/lsp/server.go
index 28abbe6a9..8c1b35f9a 100644
--- a/internal/lsp/server.go
+++ b/internal/lsp/server.go
@@ -1,5 +1,7 @@
 package lsp
 
+import "github.com/microsoft/typescript-go/internal/golarext"
+
 import (
 	"context"
 	"errors"
@@ -33,6 +35,8 @@ import (
 )
 
 type ServerOptions struct {
+	GolarCallbacks *golarext.GolarCallbacks
+
 	In  Reader
 	Out Writer
 	Err io.Writer
@@ -51,6 +55,7 @@ func NewServer(opts *ServerOptions) *Server {
 	}
 
 	s := &Server{
+		golarCallbacks: opts.GolarCallbacks,
 		r:                     opts.In,
 		w:                     opts.Out,
 		stderr:                opts.Err,
@@ -133,6 +138,8 @@ var (
 )
 
 type Server struct {
+	golarCallbacks *golarext.GolarCallbacks
+
 	r             Reader
 	w             Writer
 	backgroundCtx context.Context
@@ -970,6 +977,8 @@ func (s *Server) handleInitialized(ctx context.Context, params *lsproto.Initiali
 	s.session = project.NewSession(&project.SessionInit{
 		BackgroundCtx: s.backgroundCtx,
 		Options: &project.SessionOptions{
+			GolarCallbacks: s.golarCallbacks,
+
 			CurrentDirectory:       cwd,
 			DefaultLibraryPath:     s.defaultLibraryPath,
 			TypingsLocation:        s.typingsLocation,
diff --git a/internal/module/resolver.go b/internal/module/resolver.go
index 89f885b4b..eba460454 100644
--- a/internal/module/resolver.go
+++ b/internal/module/resolver.go
@@ -1448,6 +1448,14 @@ func (r *resolutionState) tryAddingExtensions(extensionless string, extensions e
 		}
 		return continueSearching()
 	default:
+		for _, e := range tspath.ExtraExtensions {
+			if e == originalExtension {
+				if resolved := r.tryExtension(originalExtension, extensionless, false, onlyRecordFailures); !resolved.shouldContinueSearching() {
+					return resolved
+				}
+				return continueSearching()
+			}
+		}
 		if extensions&extensionsDeclaration != 0 && !tspath.IsDeclarationFileName(extensionless+originalExtension) {
 			if resolved := r.tryExtension(".d"+originalExtension+".ts", extensionless, false, onlyRecordFailures); !resolved.shouldContinueSearching() {
 				return resolved
diff --git a/internal/module/util.go b/internal/module/util.go
index bb48dedcb..036400eb9 100644
--- a/internal/module/util.go
+++ b/internal/module/util.go
@@ -169,6 +169,11 @@ func GetResolutionDiagnostic(options *core.CompilerOptions, resolvedModule *Reso
 	case tspath.ExtensionJson:
 		return needResolveJsonModule()
 	default:
+		for _, e := range tspath.ExtraExtensions {
+			if e == resolvedModule.Extension {
+				return nil
+			}
+		}
 		return needAllowArbitraryExtensions()
 	}
 }
diff --git a/internal/project/autoimport.go b/internal/project/autoimport.go
index 9dcc8228f..f863fcbe2 100644
--- a/internal/project/autoimport.go
+++ b/internal/project/autoimport.go
@@ -158,7 +158,7 @@ func (a *autoImportRegistryCloneHost) GetSourceFile(fileName string, path tspath
 		JSDocParsingMode: ast.JSDocParsingModeParseAll,
 	}
 	key := NewParseCacheKey(opts, fh.Hash(), fh.Kind())
-	result := a.parseCache.Load(key, fh)
+	result := a.parseCache.Load(key, ParseCacheArgs{fh: fh, fs: a.fs})
 
 	a.filesMu.Lock()
 	a.files = append(a.files, key)
diff --git a/internal/project/compilerhost.go b/internal/project/compilerhost.go
index 1af21a828..14f033f7f 100644
--- a/internal/project/compilerhost.go
+++ b/internal/project/compilerhost.go
@@ -95,7 +95,7 @@ func (c *compilerHost) GetResolvedProjectReference(fileName string, path tspath.
 func (c *compilerHost) GetSourceFile(opts ast.SourceFileParseOptions) *ast.SourceFile {
 	c.ensureAlive()
 	if fh := c.sourceFS.GetFileByPath(opts.FileName, opts.Path); fh != nil {
-		return c.builder.parseCache.Load(NewParseCacheKey(opts, fh.Hash(), fh.Kind()), fh)
+		return c.builder.parseCache.Load(NewParseCacheKey(opts, fh.Hash(), fh.Kind()), ParseCacheArgs{fh: fh, fs: c.sourceFS})
 	}
 	return nil
 }
diff --git a/internal/project/parsecache.go b/internal/project/parsecache.go
index 2ca96a188..cc0b230ec 100644
--- a/internal/project/parsecache.go
+++ b/internal/project/parsecache.go
@@ -3,7 +3,9 @@ package project
 import (
 	"github.com/microsoft/typescript-go/internal/ast"
 	"github.com/microsoft/typescript-go/internal/core"
+	"github.com/microsoft/typescript-go/internal/golarext"
 	"github.com/microsoft/typescript-go/internal/parser"
+	"github.com/microsoft/typescript-go/internal/vfs"
 	"github.com/zeebo/xxh3"
 )
 
@@ -25,14 +27,23 @@ func NewParseCacheKey(
 	}
 }
 
-type ParseCache = RefCountCache[ParseCacheKey, *ast.SourceFile, FileHandle]
+type ParseCacheArgs struct {
+	fh FileHandle
+	fs vfs.FS
+}
+type ParseCache = RefCountCache[ParseCacheKey, *ast.SourceFile, ParseCacheArgs]
 
-func NewParseCache(options RefCountCacheOptions) *ParseCache {
+func NewParseCache(options RefCountCacheOptions, golarCallbacks *golarext.GolarCallbacks) *ParseCache {
 	return NewRefCountCache(
 		options,
-		func(key ParseCacheKey, fh FileHandle) *ast.SourceFile {
-			file := parser.ParseSourceFile(key.SourceFileParseOptions, fh.Content(), key.ScriptKind)
-			file.Hash = fh.Hash()
+		func(key ParseCacheKey, args ParseCacheArgs) *ast.SourceFile {
+			var file *ast.SourceFile
+			if golarCallbacks == nil {
+				file = parser.ParseSourceFile(key.SourceFileParseOptions, args.fh.Content(), key.ScriptKind)
+			} else {
+				file = golarCallbacks.ParseSourceFile(args.fs, key.SourceFileParseOptions, args.fh.Content(), key.ScriptKind)
+			}
+			file.Hash = args.fh.Hash()
 			return file
 		},
 		nil,
diff --git a/internal/project/session.go b/internal/project/session.go
index b289cd759..bacaea91a 100644
--- a/internal/project/session.go
+++ b/internal/project/session.go
@@ -9,10 +9,12 @@ import (
 	"sync/atomic"
 	"time"
 
+	"github.com/NikhilVerma/vue-tsgo/golar"
 	"github.com/microsoft/typescript-go/internal/ast"
 	"github.com/microsoft/typescript-go/internal/collections"
 	"github.com/microsoft/typescript-go/internal/compiler"
 	"github.com/microsoft/typescript-go/internal/core"
+	"github.com/microsoft/typescript-go/internal/golarext"
 	"github.com/microsoft/typescript-go/internal/locale"
 	"github.com/microsoft/typescript-go/internal/ls"
 	"github.com/microsoft/typescript-go/internal/ls/lsconv"
@@ -42,6 +44,8 @@ const (
 // SessionOptions are the immutable initialization options for a session.
 // Snapshots may reference them as a pointer since they never change.
 type SessionOptions struct {
+	GolarCallbacks *golarext.GolarCallbacks
+
 	CurrentDirectory       string
 	DefaultLibraryPath     string
 	TypingsLocation        string
@@ -142,7 +146,7 @@ func NewSession(init *SessionInit) *Session {
 	overlayFS := newOverlayFS(init.FS, make(map[tspath.Path]*Overlay), init.Options.PositionEncoding, toPath)
 	parseCache := init.ParseCache
 	if parseCache == nil {
-		parseCache = NewParseCache(RefCountCacheOptions{})
+		parseCache = NewParseCache(RefCountCacheOptions{}, golar.GolarExtCallbacks)
 	}
 	extendedConfigCache := NewExtendedConfigCache()
 
diff --git a/internal/project/snapshot.go b/internal/project/snapshot.go
index 91e161ec0..c48b31dca 100644
--- a/internal/project/snapshot.go
+++ b/internal/project/snapshot.go
@@ -1,5 +1,7 @@
 package project
 
+import "github.com/microsoft/typescript-go/internal/golarext"
+
 import (
 	"context"
 	"fmt"
@@ -47,6 +49,10 @@ type Snapshot struct {
 	apiError    error
 }
 
+func (s *Snapshot) GetGolarCallbacks() *golarext.GolarCallbacks {
+	return s.sessionOptions.GolarCallbacks
+}
+
 // NewSnapshot
 func NewSnapshot(
 	id uint64,
diff --git a/internal/tspath/extension.go b/internal/tspath/extension.go
index 7b6578363..5065806e2 100644
--- a/internal/tspath/extension.go
+++ b/internal/tspath/extension.go
@@ -23,6 +23,7 @@ const (
 )
 
 var (
+	ExtraExtensions                                = []string{}
 	SupportedDeclarationExtensions                 = []string{ExtensionDts, ExtensionDcts, ExtensionDmts}
 	SupportedTSImplementationExtensions            = []string{ExtensionTs, ExtensionTsx, ExtensionMts, ExtensionCts}
 	supportedTSExtensionsForExtractExtension       = []string{ExtensionDts, ExtensionDcts, ExtensionDmts, ExtensionTs, ExtensionTsx, ExtensionMts, ExtensionCts}
@@ -37,6 +38,17 @@ var (
 	ExtensionsNotSupportingExtensionlessResolution = []string{ExtensionMts, ExtensionDmts, ExtensionMjs, ExtensionCts, ExtensionDcts, ExtensionCjs}
 )
 
+
+func RegisterSupportedExtension(ext string) {
+	ExtraExtensions = append(ExtraExtensions, ext)
+	AllSupportedExtensions[0] = append(AllSupportedExtensions[0], ext)
+	AllSupportedExtensionsWithJson[0] = append(AllSupportedExtensionsWithJson[0], ext)
+	SupportedTSExtensions[0] = append(SupportedTSExtensions[0], ext)
+	SupportedTSExtensionsWithJson[0] = append(SupportedTSExtensionsWithJson[0], ext)
+	SupportedTSExtensionsFlat = append(SupportedTSExtensionsFlat, ext)
+	SupportedTSExtensionsWithJsonFlat = append(SupportedTSExtensionsWithJsonFlat, ext)
+}
+
 func ExtensionIsTs(ext string) bool {
 	return ext == ExtensionTs || ext == ExtensionTsx || ext == ExtensionDts || ext == ExtensionMts || ext == ExtensionDmts || ext == ExtensionCts || ext == ExtensionDcts || len(ext) >= 7 && ext[:3] == ".d." && ext[len(ext)-3:] == ".ts"
 }
-- 
2.49.0

