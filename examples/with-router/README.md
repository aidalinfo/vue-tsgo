# Vue 3 + Vue Router + TypeScript Example

*Coming soon*

This example will demonstrate vue-tsgo type checking with Vue Router.

## Planned Features

- Typed route definitions
- Route parameter inference
- Navigation guard types
- Router composables (`useRoute`, `useRouter`)
- Lazy-loaded components

## Placeholder

For now, see the [basic example](../basic/) for fundamental vue-tsgo usage.
